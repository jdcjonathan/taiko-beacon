{
  "name": "context-pack",
  "version": "1.0.0",
  "private": true,
  "description": "Taiko AI context pack and AI.md template",
  "type": "module",
  "scripts": {
    "build": "tsc && pnpm run generate",
    "generate": "node dist/generate.js",
    "generate-ai": "node dist/generate-ai-md.js",
    "test": "pnpm run build && node --test test/*.test.js",
    "lint": "echo 'lint ok'"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
