{
  "name": "entity-corrections",
  "version": "1.0.0",
  "private": true,
  "description": "Track external entity listing corrections (CMC, DeFiLlama, etc.)",
  "scripts": {
    "build": "echo 'entity-corrections build'",
    "test": "node -e \"require('fs'); JSON.parse(require('fs').readFileSync('corrections.json','utf8'))\"",
    "lint": "echo 'lint ok'",
    "status": "python check-status.py"
  }
}
