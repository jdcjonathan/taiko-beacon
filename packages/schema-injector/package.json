{
  "name": "schema-injector",
  "version": "1.0.0",
  "private": true,
  "description": "Inject FAQPage JSON-LD into docs.taiko.xyz HTML",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "inject": "node dist/inject.js",
    "test": "pnpm run build && node --test test/*.test.js",
    "lint": "echo 'lint ok'"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
