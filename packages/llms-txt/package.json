{
  "name": "llms-txt",
  "version": "1.0.0",
  "private": true,
  "description": "Generate llms.txt and llms-full.txt from canonical entity definition",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "node dist/generate.js",
    "test": "pnpm run build && pnpm run generate && node --test test/generate.test.js",
    "lint": "echo 'lint ok'"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
